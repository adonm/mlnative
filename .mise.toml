[tools]
python = "3.12"
rust = "1.90.0"
just = "1.48.1"
uv = "0.11.3"

[env]
PYTHONPATH = "{{config_root}}"

[tasks]
setup = "just setup"
test = "just test"
lint = "just lint"
format = "just format"
typecheck = "just typecheck"
check = "just check"
build = "just build"
clean = "just clean"
