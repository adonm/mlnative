[tools]
python = "3.14.6"
rust = "1.94.1"
just = "1.49.0"
uv = "0.11.6"

[env]
PYTHONPATH = "{{config_root}}"

[tasks]
setup = "just setup"
test = "just test"
lint = "just lint"
format = "just format"
typecheck = "just typecheck"
check = "just check"
build = "just build"
clean = "just clean"
